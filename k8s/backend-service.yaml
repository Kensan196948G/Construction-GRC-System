apiVersion: v1
kind: Service
metadata:
  name: grc-backend
  namespace: grc-system
  labels:
    app.kubernetes.io/name: grc-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: grc-system
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: grc-backend
